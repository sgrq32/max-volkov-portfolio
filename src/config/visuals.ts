// Add real, approved assets in public/ and set their paths here.
// Empty paths intentionally render abstract branding, never invented app screens or faces.
export const visuals: {portrait: string; neryoScreens: [string, string]} = {
  portrait: '',
  neryoScreens: ['', ''],
};
